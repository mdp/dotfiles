# ~/.bashrc: executed by bash(1) for non-login shells.
# see /usr/share/doc/bash/examples/startup-files (in the package bash-doc)
# for examples

# If not running interactively, don't do anything
case $- in
    *i*) ;;
      *) return;;
esac

#
# Omarchy setup: use system omarchy if available, otherwise use chezmoi'd copy
if [ -f ~/.local/share/omarchy/default/bash/rc ]; then
    # On an omarchy machine - source from system installation
    source ~/.local/share/omarchy/default/bash/rc
elif [ -f ~/.bash/rc ]; then
    # Not on an omarchy machine - use chezmoi'd copy
    source ~/.bash/rc
fi

# Alias definitions.
if [ -f ~/.aliasrc ]; then
    . ~/.aliasrc
fi


